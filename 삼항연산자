#include<stdio.h>

//문제는 a, b를 비교하여 둘의 차를 구하기
int main(){
int a,b;
int imsum;
scanf("%d %d", &a ,&b);

//삼항연산자 (a = b) ? a : b
//(조건문)이 참이면 a 거짓이면 b를 출력
imsum = (a > b) ? a - b : b - a;
printf("%d", imsum);
//두 숫자의 차를 출력

return 0;
}
